# 规则版新闻事件抽取 — 技术设计文档

## 一、模块定位

**任务目标**：从一条国际新闻的标题、正文和来源中，基于规则、关键词和正则表达式（不调用外部 AI API），抽取出结构化事件对象，供后续"股票匹配、ReLU 动量、影响评分"模块消费。

**输入**：

```text
headline: Tesla announces $10 billion battery plant in Saudi Arabia
body:     The new factory will expand Tesla's energy storage and EV battery production.
source:   Reuters
```

**输出**：

```json
{
  "eventType": "国际项目",
  "sector": "新能源",
  "country": "Saudi Arabia",
  "projectAmountUsd": 10000000000,
  "companies": ["Tesla"],
  "keywords": ["battery", "ev", "energy storage", "plant", "factory"],
  "sourceCredibility": 0.9
}
```

**模块边界**：本 Service 只负责"新闻 → 结构化事件"。股票代码匹配属于任务 4，涨跌方向与评分属于后续 ReLU / 影响评分模块，本模块不涉及。

---

## 二、字段说明

| 字段 | 类型 | 作用 |
|---|---|---|
| `eventType` | String | 判断新闻属于项目、订单、政策、风险等哪类事件 |
| `sector` | String | 判断影响行业，如新能源、AI 芯片、半导体 |
| `country` | String | 事件发生国家或地区 |
| `projectAmountUsd` | Long / BigDecimal | 项目金额，统一换算为美元数值 |
| `companies` | List\<String\> | 涉及公司名称 |
| `keywords` | List\<String\> | 实际命中的规则关键词，用于解释与调试 |
| `sourceCredibility` | Double | 新闻源可信度评分（0~1） |

---

## 三、整体流程

```text
1. 合并标题和正文
2. 文本标准化
3. 金额抽取
4. 行业识别
5. 事件类型识别
6. 国家/地区识别
7. 公司名称抽取
8. 新闻源可信度打分
9. 组装 ExtractedEvent
```

接口签名：

```java
ExtractedEvent extract(String headline, String body, String source);
```

---

## 四、各步骤详细设计

### 4.1 文本标准化

```java
String text = (headline + " " + body).toLowerCase();
```

- 合并标题与正文，统一转小写用于关键词匹配
- 保留原文副本，用于公司名等大小写敏感的抽取
- 去除多余空格、处理空标题 / 空正文的边界情况，避免 NPE

### 4.2 金额抽取

**支持写法**：

| 原文 | 结果 |
|---|---:|
| `$10B` | 10000000000 |
| `$10 billion` | 10000000000 |
| `USD 2.5 billion` | 2500000000 |
| `$500 million` | 500000000 |
| `10bn dollars` | 10000000000 |

**正则示例**：

```text
\$?\s?(\d+(\.\d+)?)\s?(b|bn|billion)
\$?\s?(\d+(\.\d+)?)\s?(m|mn|million)
usd\s?(\d+(\.\d+)?)\s?(billion|million)
```

**换算规则**：

- `b / bn / billion` → × 1,000,000,000
- `m / mn / million` → × 1,000,000

**多金额策略**：一篇新闻出现多个金额时，第一版取**最大值**（项目金额通常是文中最大金额）。例如 `$200 million supply deal for a $5 billion project` → 取 `$5 billion`。

### 4.3 行业识别

基于关键词映射，统计命中次数，取命中最多的行业：

| 关键词 | 行业 |
|---|---|
| `solar`, `battery`, `ev`, `energy storage` | 新能源 |
| `gpu`, `ai chip`, `data center`, `accelerator` | AI 芯片 |
| `semiconductor`, `foundry`, `lithography`, `wafer` | 半导体 |
| `oil`, `gas`, `lng` | 能源 |
| `cloud`, `server`, `data center` | 云计算/数据中心 |

```java
Map<String, List<String>> sectorKeywords = Map.of(
    "新能源", List.of("solar", "battery", "ev", "energy storage"),
    "AI芯片", List.of("gpu", "ai chip", "data center"),
    "半导体", List.of("semiconductor", "foundry", "lithography")
);
```

无命中时返回 `"待确认"`，不编造行业。

### 4.4 事件类型识别

| 关键词 | 事件类型 |
|---|---|
| `project`, `factory`, `plant`, `build`, `facility` | 国际项目 |
| `contract`, `order`, `deal`, `supply agreement` | 订单合同 |
| `subsidy`, `policy`, `regulation`, `tax credit` | 政策监管 |
| `ban`, `sanction`, `lawsuit`, `probe`, `recall` | 风险事件 |
| `investment`, `partnership`, `joint venture` | 投资合作 |

**优先级**（风险/政策对影响方向更敏感，优先判定）：

```text
风险事件 > 政策监管 > 订单合同 > 国际项目 > 投资合作
```

示例：`US bans Nvidia AI chip exports` → 虽含 `AI chip`，但事件类型应为"风险事件"。

### 4.5 国家/地区识别

MVP 使用静态国家词典匹配：

```java
Map<String, String> countryAliases = Map.of(
    "saudi arabia", "Saudi Arabia",
    "u.s.", "United States",
    "us", "United States",
    "usa", "United States",
    "china", "China",
    "germany", "Germany",
    "japan", "Japan"
);
```

未识别到返回 `"待确认"`。第一版不引入复杂 NLP 地名识别。

### 4.6 公司名称抽取

MVP 以**静态公司词典**为主（可与任务 4 股票匹配复用同一份词典）：

```java
Map<String, List<String>> companyAliases = Map.of(
    "Tesla",  List.of("tesla", "tsla"),
    "Nvidia", List.of("nvidia", "nvda"),
    "AMD",    List.of("amd", "advanced micro devices"),
    "ASML",   List.of("asml"),
    "TSMC",   List.of("tsmc", "taiwan semiconductor")
);
```

不建议 MVP 依赖"连续大写开头单词"等启发式规则，误判率高。

### 4.7 关键词输出

`keywords` 记录**实际命中的规则关键词**（含行业词、事件词、金额原文、国家、公司），而非全文分词。例如：

```json
"keywords": ["battery", "ev", "energy storage", "plant", "factory", "$10 billion", "Saudi Arabia", "Tesla"]
```

价值：前端可展示"系统为何如此判断"，后端可追溯规则命中路径。

### 4.8 新闻源可信度

内置媒体可信度表：

```java
Map<String, Double> sourceCredibility = Map.of(
    "Reuters", 0.90,
    "Bloomberg", 0.88,
    "Associated Press", 0.86,
    "Financial Times", 0.85,
    "CNBC", 0.78,
    "Unknown", 0.50
);
```

来源不在表中时返回默认值 `0.50`（或 `0.60`），**不返回 0**——"不认识来源"不等于"不可信"。

---

## 五、数据结构

```java
public record ExtractedEvent(
    String eventType,
    String sector,
    String country,
    Long projectAmountUsd,       // 或 BigDecimal
    List<String> companies,
    List<String> keywords,
    Double sourceCredibility
) {}
```

金额若只需美元整数，用 `Long` 更简洁；需小数精度则用 `BigDecimal`。

---

## 六、完整示例

**输入**：

```text
headline: Tesla announces $10 billion battery plant in Saudi Arabia
body:     The new factory will expand Tesla's energy storage and EV battery production.
source:   Reuters
```

**抽取过程**：

| 步骤 | 结果 |
|---|---|
| 金额 | `$10 billion` → 10000000000 |
| 行业 | `battery`, `ev`, `energy storage` → 新能源 |
| 事件类型 | `plant`, `factory` → 国际项目 |
| 国家 | `Saudi Arabia` |
| 公司 | `Tesla` |
| 来源可信度 | Reuters → 0.90 |

**输出**：

```json
{
  "eventType": "国际项目",
  "sector": "新能源",
  "country": "Saudi Arabia",
  "projectAmountUsd": 10000000000,
  "companies": ["Tesla"],
  "keywords": ["battery", "ev", "energy storage", "plant", "factory"],
  "sourceCredibility": 0.9
}
```

---

## 七、验收标准

- [ ] 从标题和正文抽取金额，支持 `B / billion / million` 等写法
- [ ] 识别新能源、AI 芯片、半导体等行业（命中次数最多者优先）
- [ ] 识别国际项目、订单合同、政策监管、风险事件（按优先级判定）
- [ ] 抽取公司名称（静态词典为主）
- [ ] 识别不到的字段返回 `"待确认"`，不编造数据
- [ ] 根据新闻源返回可信度评分，未知来源返回默认值而非 0
- [ ] 模块边界清晰：不做股票匹配，不做涨跌评分
