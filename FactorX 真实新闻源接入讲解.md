# FactorX 真实新闻源接入讲解

## 1. 新闻源选型建议

第一版建议选择 **RSS + Finnhub** 作为主要新闻源：

| 新闻源 | 特点 | 定位 |
|--------|------|------|
| RSS | 免费、稳定，适合 Reuters、SEC、公司 IR 等公开订阅源 | 主力源 |
| Finnhub | 提供结构化财经新闻，便于按股票或分类拉取 | 主力源 |
| GDELT | 覆盖面广，但噪声和转载较多 | 第三个源（后续接入） |

---

## 2. 完整链路

```
定时任务
  -> 拉取各新闻源增量数据
  -> 统一字段格式
  -> 清洗标题、摘要、URL、时间
  -> 去重
  -> 入库
  -> 异步触发 /api/analyze 等分析服务
  -> 前端新闻流轮询或实时刷新
```

---

## 3. 关键原则

### 3.1 适配器隔离供应商差异

每个新闻源各自实现 `NewsSourceAdapter`，转换成内部统一对象：

- `title`
- `body`
- `source`
- `url`
- `publishedAt`
- `language`
- `region`

**业务分析层不能直接依赖 Finnhub 或 RSS 的原始字段。**

---

### 3.2 增量抓取

- Spring Scheduler 每 **5 分钟**执行一次
- 请求时按"上次成功抓取时间"或供应商提供的时间范围拉取
- 避免重复拉全量历史新闻

---

### 3.3 清洗

- 去 HTML、无意义空白和 URL 跟踪参数
- 统一时间格式为 **UTC**
- 标题、URL 或发布时间缺失的新闻**不进入自动分析**
- 正文只有摘要时应如实保存摘要

---

### 3.4 去重

**最低实现：**

```
sha256(title + source + publishedAt)
```

**更可靠的三层去重：**

1. **同源**：`source + externalId`
2. **同链接**：规范化 URL
3. **跨源转载**：标题和摘要内容哈希或相似度判断

---

### 3.5 先入库，后分析

抓取新闻的任务**不应同步执行 AI 或量化分析**，否则一个慢请求会阻塞后续抓取。

新闻保存成功后，再**异步触发**：

- 事件抽取
- 股票匹配
- 评分

---

### 3.6 状态管理

新闻应有处理状态，例如：

| 状态 | 含义 |
|------|------|
| RECEIVED | 已接收 |
| ANALYZING | 分析中 |
| ANALYZED | 已分析 |
| FAILED | 分析失败 |

前端显示"处理中"，而不是将尚未分析的新闻误判为"无影响"。

---

### 3.7 限流和失败恢复

为每个供应商设置：

- 超时
- QPS 限制
- 每日额度
- 重试策略

**具体策略：**

- 对 429 和网络超时使用**指数退避**
- 鉴权错误直接记录并告警
- 失败新闻可限制重试 **3 次**

---

### 3.8 密钥安全

Finnhub、NewsAPI 等 Key 放入**环境变量**，例如：

```
FINNHUB_API_KEY
```

**不能写进：**

- `application.properties`
- Git 仓库
- 前端代码

---

## 4. 建议接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/news?page=&source=&from=&to=` | 分页查询新闻列表 |
| GET | `/api/news/{id}` | 查询单条新闻详情 |
| POST | `/api/news/{id}/reanalyze` | 重新分析指定新闻 |

---

## 5. 验收重点

验收时重点验证以下场景：

1. **不同新闻源的字段映射** — 各适配器是否正确转换为统一内部对象
2. **同一篇新闻重复拉取** — 同源去重是否生效
3. **跨源转载** — 不同来源的同一篇新闻是否能识别去重
4. **超时与限流** — 供应商限流时是否正确退避和恢复
5. **新闻分析失败后的重试** — 失败新闻是否按策略重试，超过次数后标记 FAILED
6. **新闻流展示** — 是否能正确展示来源、发布时间和分析状态
