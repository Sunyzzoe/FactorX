# 任务 6：因子激活与评分模型 设计文档

> 版本：MVP v1.0  
> 定位：规则型、可解释的评分器（非历史校准的概率预测模型）

---

## 一、任务定位

"因子激活与评分模型"负责把上游的非结构化新闻、事件抽取结果、股票关联关系和行情动量，统一转换为每只股票的以下输出：

| 输出字段 | 说明 |
|---------|------|
| **影响方向** | 利好 / 利空 / 中性 |
| **影响置信度** | 45% – 85% |
| **预估涨跌幅区间** | 基于评分映射的 low ~ high |
| **影响周期** | 3 – 10 个交易日 |
| **可解释因子明细** | 每项因子的原始分数、阈值、激活值、参与原因 |

> **关键定位**：本模型是一个规则型、可解释的 MVP 评分器，**不是**经过历史样本训练和校准的"真实概率预测模型"。

---

## 二、完整数据流

```
新闻
  → 事件抽取
      → 事件类型、行业、金额、公司、新闻源可信度
  → 股票匹配
      → 每只股票的 relevance（关联度）
  → ReLU 动量
      → momentumPurity、positiveDensity、plateauRatio 等
  → 因子标准化与激活
  → 加权得到 finalImpactScore
  → 映射为概率、涨跌幅、方向与风险说明
```

对某一只股票而言，数据流在回答三个不同层次的问题：

| 层次 | 关键问题 | 典型输入 |
|------|---------|---------|
| **事件层** | 新闻本身是否有足够影响力？ | 项目金额、事件类型、行业、来源 |
| **关联层** | 这条新闻为什么会影响这只股票？ | relevance、供应链/竞争/行业关系 |
| **市场层** | 市场是否具备把信息转化为价格走势的条件？ | ReLU 动量、量价确认 |

- 新闻告诉系统"**可能影响谁**"；
- 股票关联度说明"**影响路径有多直接**"；
- ReLU 动量与市场确认判断"**这条信息是否更可能形成价格反应**"。

---

## 三、六个原始因子

第一版拆分为六项原始因子：

| 因子 | 原始数据 | 评分含义 |
|------|---------|---------|
| **国际项目规模** | projectAmountUsd | 金额或项目规模越大，潜在基本面影响通常越高 |
| **新闻源可信度** | sourceCredibility | Reuters、Bloomberg 等高可信来源能降低事件真实性的不确定性 |
| **股票关联度** | MatchedStock.relevance | 新闻事件与个股主营业务、供应链或竞争关系的直接程度 |
| **行业景气** | 行业热度或行业是否明确 | 行业处于高景气或明确受益时，事件更可能传导至个股 |
| **市场确认** | 成交量、价格趋势等 | 新闻出现后市场是否已有资金与价格行为的配合 |
| **ReLU 动量** | momentumPurity 等 | 股票是否已经形成经过噪声过滤的正向动量结构 |

### 3.1 归一化

所有原始分数先归一化到 `[0, 1]`：

| 分数 | 含义 |
|------|------|
| 0 | 没有支持证据或完全不成立 |
| 0.5 | 信息不完整或影响一般 |
| 1 | 证据充分、关联强、确认度高 |

### 3.2 项目规模的对数压缩

项目规模使用对数函数，避免极大金额线性拉爆分数：

```
projectScore = 0.25 + log10(1 + amountBillion) * 0.48
```

- 项目从 1B 增加到 10B 会提高评分，但不会机械地提高十倍；
- 这符合市场影响的**边际递减**规律。

---

## 四、因子激活机制

### 4.1 激活公式

原始分数不等于最终贡献。因子激活是一个"**低于证据门槛则不贡献**"的机制：

```
activation = max(0, rawScore - threshold) / (1 - threshold)
```

| 条件 | 结果 |
|------|------|
| rawScore ≤ threshold | activation = 0 |
| rawScore > threshold | 因子开始产生贡献 |
| rawScore = 1 | activation = 1 |

输出仍然位于 `[0, 1]`。

### 4.2 示例（阈值 = 0.50）

| rawScore | 计算 | activation | 解释 |
|----------|------|------------|------|
| 0.35 | max(0, 0.35 - 0.50) / 0.50 | 0.00 | 证据不足，不激活 |
| 0.50 | 0 / 0.50 | 0.00 | 刚好达门槛，仍不贡献 |
| 0.65 | 0.15 / 0.50 | 0.30 | 有一定确认，部分激活 |
| 0.90 | 0.40 / 0.50 | 0.80 | 证据强，显著激活 |
| 1.00 | 0.50 / 0.50 | 1.00 | 完全激活 |

### 4.3 为什么用激活而非原始分

- **抑制噪声**：弱信号不会造成"看起来有分"的假象；
- **反映金融判断门槛**：没有达到可信、关联或动量的最低标准，就不应显著推高结论；
- **便于解释**：用户可以明确看到是"分数低"，还是"没有跨过阈值"。

### 4.4 当前实现状态

- `ReluFactorService.java` 已对 ReLU 子因子实现激活函数，向前端返回 `rawScore`、`threshold`、`activation` 和 `reason`；
- 前端 `FactorActivationPanel.tsx` 将 `activation` 渲染为进度条。

---

## 五、ReLU 动量因子

### 5.1 核心思想

ReLU 动量**不直接看累计涨跌**，而是只保留超过日收益阈值的正收益：

```
dailyReturn  = ln(price[t] / price[t - 1])
reluReturn   = max(0, dailyReturn - threshold)
reluMomentum = sum(reluReturn)
```

当前默认日阈值 `threshold = 0.004`（约 0.4%）：

| 日收益 | 处理 |
|--------|------|
| +0.2% | 低于阈值，按 0 处理 |
| +1.0% | 保留约 +0.6% |
| -2.0% | 按 0 处理 |

> 它追踪的是"**超过最小有效波动后的正向动量积累**"，而非完整收益率，能弱化低幅震荡的影响。

### 5.2 衍生指标

| 指标 | 计算或含义 |
|------|-----------|
| reluSlope | reluMomentum / lookbackDays，有效动量的日均斜率 |
| positiveDensity | 超过阈值的交易日占比 |
| plateauRatio | 1 - positiveDensity，停留在零激活区的比例 |
| momentumPurity | reluSlope × positiveDensity × (1 - plateauRatio) |

由于 `1 - plateauRatio = positiveDensity`，公式可化简为：

```
momentumPurity = reluSlope × positiveDensity²
```

因此它偏好同时具备以下特征的股票：

1. 有持续的、阈值以上的正收益；
2. 有效上涨日的比例较高；
3. 不是由少数几天的大涨支撑，而是具有**连续性**。

### 5.3 转换为总评分用的 momentumScore

```
momentumScore = clamp(momentumPurity * 100 * 1.25 + positiveDensity * 0.25)
```

`clamp` 将结果限制在 `[0, 1]`。

---

## 六、综合评分

### 6.1 五项汇总公式

```
finalImpactScore =
      0.30 × eventScore
    + 0.25 × stockRelevanceScore
    + 0.20 × reluMomentumScore
    + 0.15 × sourceCredibilityScore
    + 0.10 × marketConfirmationScore
```

权重之和 = 1.00。

| 汇总项 | 权重 | 作用 |
|--------|------|------|
| eventScore | 30% | 事件本身的经济意义与质量 |
| stockRelevanceScore | 25% | 个股是否是直接受益者、受损者或强相关标的 |
| reluMomentumScore | 20% | 是否具备正向价格结构确认 |
| sourceCredibilityScore | 15% | 新闻真实性和信息质量 |
| marketConfirmationScore | 10% | 市场量价层面的确认 |

> "六个因子"和"五项总公式"并不冲突：项目规模、行业景气、公司明确性等先聚合为 `eventScore`，其余关键因子直接进入总分。

### 6.2 eventScore 的内部结构（两层评分）

```
eventScore =
      0.30 × 项目规模
    + 0.20 × 新闻源可信度
    + 0.20 × 公司明确性
    + 0.15 × 行业明确/热度
    + 0.15 × 市场确认
```

形成"**事件内评分 → 个股影响评分**"的两层结构。当前 `ScoringService.java` 即采用此公式。

---

## 七、从评分映射到概率与涨跌幅

### 7.1 置信度映射

综合分数限制在 `[0, 1]` 后：

```
probability = 45 + finalImpactScore × 40
```

| finalImpactScore | probability |
|------------------|-------------|
| 0.00 | 45% |
| 0.50 | 65% |
| 1.00 | 85% |

> **产品命名建议**：该 `probability` 更准确的名称是"**在当前因子条件下，新闻对该股形成方向性价格反应的规则型置信度**"。它**不应**被理解为已被历史回测校准的统计概率。真正的概率模型需要以历史事件为样本，比较预测与未来 3/5/10 日真实收益，再做校准。

### 7.2 涨跌幅区间映射

```
lowMove  = 0.5 + finalImpactScore × 2
highMove = lowMove + 1.5 + finalImpactScore × 3
```

| finalImpactScore | lowMove | highMove |
|------------------|---------|----------|
| 0.00 | 0.50% | 2.00% |
| 0.50 | 1.50% | 4.50% |
| 1.00 | 2.50% | 7.00% |

### 7.3 方向决定符号

| 方向 | 输出格式 |
|------|---------|
| 利好 | +lowMove% ~ +highMove% |
| 利空 | -lowMove% ~ -highMove% |
| 中性 | 输出"方向不明确"或极窄的双向区间，**不应复用利好格式** |

---

## 八、方向判定

`finalImpactScore` 表示**影响强度和置信度**，**不负责决定正负方向**。方向由事件语义决定：

| 事件方向 | 动量状态 | 输出建议 |
|---------|---------|---------|
| 利好 | 强 | 高概率利好 |
| 利好 | 弱 | 利好，但价格确认不足 |
| 利空 | 弱 | 利空概率上升 |
| 利空 | 强 | 利空事件与既有上涨趋势冲突，应突出风险提示 |
| 中性 | 任意 | 不应强行给出单边涨跌判断 |

### 当前实现与局限

当前用标题和正文中的英文关键词判断方向：

- **利好**：contract、investment、project 等；
- **利空**：ban、sanction、lawsuit、delay 等。

这适合演示阶段，但中文新闻、否定表达、复杂因果和多事件新闻都会造成误判。**后续应替换为结构化事件的 `eventPolarity` 字段**，而非继续依赖全文关键词。

---

## 九、完整示例

### 输入

> Reuters 报道，某新能源公司获得 20 亿美元海外项目，匹配到某供应链公司。

### 因子分数

| 因子 | 分数/状态 |
|------|----------|
| 项目规模 | 0.65 |
| 新闻源可信度 | 0.90 |
| 公司明确度 | 0.80 |
| 行业明确度 | 0.72 |
| 事件层 eventScore | ≈ 0.73 |
| 个股关联度 | 0.82 |
| ReLU 动量分 | 0.70 |
| 市场确认 | 0.60 |

### 综合评分计算

```
finalImpactScore =
      0.30 × 0.73
    + 0.25 × 0.82
    + 0.20 × 0.70
    + 0.15 × 0.90
    + 0.10 × 0.60
    = 0.7605
```

### 映射结果

```
probability = 45 + 0.7605 × 40 = 75.4% → 75%

lowMove  = 0.5 + 0.7605 × 2 = 2.02%
highMove = 2.02 + 1.5 + 0.7605 × 3 = 5.80%
```

### 最终输出

```json
{
  "symbol": "XXX",
  "direction": "利好",
  "probability": 75,
  "estimatedMove": "+2.02% ~ +5.80%",
  "horizon": "3-10个交易日"
}
```

> 附带因子明细后，用户能够看出：本次高分主要来自**高可信来源、较强的股票关联和已有的正向动量**，而不是仅仅因为"新闻看起来是利好"。

---

## 十、当前代码与目标的关键差距

当前 MVP 的基本公式已在 `ScoringService.java` 实现，ReLU 激活已在 `ReluFactorService.java` 实现，但尚未完全满足"**每只股票评分均可追溯**"的验收目标：

| # | 差距 | 说明 |
|---|------|------|
| 1 | **StockImpact 缺少完整评分分解** | API 返回的 `reluFactors` 是 ReLU 指标明细；`StockImpact` 目前没有携带项目规模、来源、关联度、行业和市场确认等完整的个股评分分解 |
| 2 | **ReLU 未按个股单独计算** | `ReluFactorService.calculate(matchedStocks)` 使用第一只匹配股票的关联度构造模拟行情，并将同一个 `reluResult` 用于所有股票。接入真实行情后，应针对每个 symbol 单独计算 ReLU 动量 |
| 3 | **中性方向的涨跌幅格式错误** | 当前方向为"中性"时，涨跌幅格式仍会使用 `+` 前缀，因为代码仅对"利空"加负号。中性输出应单独处理 |
| 4 | **市场确认分为固定值** | 当前 `marketConfirmationScore` 为固定值 0.42，尚未真正使用成交量、量比、价格趋势等数据。任务 9 接入真实行情后，这一项才会具备实际意义 |

---

## 十一、核心价值与后续方向

### 核心价值

任务 6 的核心价值**不是**给出一个看似精确的数字，而是建立一个：

- **可解释**：每项因子的来源、分数、激活状态透明可查；
- **可存档**：评分结果与因子明细可持久化，便于事后复盘；
- **可回测**：固定的规则与参数使得历史回测成为可能；
- **可逐步校准**：权重、阈值、映射函数均可迭代优化。

### 后续演进

历史回测应将以下固定参数替换为**按行业、事件类型与市场状态校准**的参数：

- 固定权重（eventScore 30%、关联度 25% 等）；
- 因子激活阈值；
- 概率映射函数（45 + score × 40）；
- 涨跌幅映射函数（lowMove / highMove）。

---

*文档结束*
