# ReLU 非对称动量曲线模块 — 技术设计文档

## 一、模块定位

该模块的作用**不是预测新闻本身**，而是确认目标股票是否已有"有效上涨动量"。它把价格序列转换成只累计超过阈值的正收益的阶梯曲线，为新闻利好是否可能兑现提供市场侧证据。

**在流水线中的位置**：新闻匹配股票之后、影响评分之前。

---

## 二、输入与输出

### 输入

| 参数 | 说明 | 示例 |
|---|---|---|
| 收盘价序列 | \(P_0, P_1, \dots, P_t\) | 日收盘价 |
| `threshold` | 噪声过滤阈值 | 0.004（0.4%） |
| `lookbackDays` | 回看窗口 | 近 20 个交易日 |

### 输出

- 原始净值 / 价格曲线
- 传统累计收益曲线
- ReLU 正向动量曲线
- 四项核心指标：`reluSlope`、`positiveDensity`、`plateauRatio`、`momentumPurity`

---

## 三、三步计算

### 第一步：对数日收益率

\[
r_t = \ln(P_t / P_{t-1})
\]

例如价格从 100 到 101：

\[
r_t = \ln(101 / 100) = 0.00995 \approx 0.995\%
\]

### 第二步：ReLU 阈值截断

\[
\text{relu}_t = \max(0,\ r_t - \text{threshold})
\]

假设阈值为 0.4%：

| 当日收益 | relu_t |
|---:|---:|
| +1.0% | 1.0% − 0.4% = **0.6%** |
| +0.2% | **0**（未过阈值） |
| −1.5% | **0**（负收益直接归零） |

### 第三步：累计有效正收益

\[
M_t = \sum_{i=1}^{t} \max(0,\ r_i - \text{threshold})
\]

因此 \(M_t\) **只会上升或横盘**，形成阶梯图：

- 强且超过门槛的上涨日 → 曲线向上跳
- 下跌日、小涨日、横盘日 → 曲线保持平台
- 不会因为下跌而下降

这就是"**非对称**"的含义：上涨和下跌并未被对称处理。负收益不被当作负向动量累加，而是作为"当前没有足够正向动量"的信号。

---

## 四、为什么不直接使用累计收益

传统累计收益会同时吸收上涨和下跌：

\[
\text{传统累计收益} = \sum r_t
\]

它回答的是"这段时间总体涨了多少"。

**ReLU 动量回答的是"这段时间有多少天出现了超过噪声门槛的有效上涨"。**

例如两只股票最终都上涨 5%：

| 股票 | 走势特征 | ReLU 曲线 |
|---|---|---|
| A | 连续多天每次涨 0.8%~1.5% | 持续上行 |
| B | 单日暴涨 8%，随后持续回落、横盘 | 仅一次明显台阶，之后长平台 |

传统收益可能相近，但 **A 的正向动量结构更连续**，新闻利好出现后更值得提高确认权重。

---

## 五、四个核心指标

### 公式定义

\[
\text{reluSlope} = \frac{M_t}{\text{lookbackDays}}
\]

\[
\text{positiveDensity} = \frac{\text{count}(r_t > \text{threshold})}{\text{lookbackDays}}
\]

\[
\text{plateauRatio} = \frac{\text{count}(r_t \le \text{threshold})}{\text{lookbackDays}}
\]

\[
\text{momentumPurity} = \text{reluSlope} \times \text{positiveDensity} \times (1 - \text{plateauRatio})
\]

### 指标含义

| 指标 | 含义 |
|---|---|
| `reluSlope` | 平均每天捕获的有效上涨幅度。越高，说明有效上涨既多又强 |
| `positiveDensity` | 窗口内有效上涨日的比例。例如 20 天中 8 天超过阈值，为 40% |
| `plateauRatio` | 没有产生有效动量的天数比例。高意味着曲线长时间横向，趋势确认不足 |
| `momentumPurity` | 将上涨强度、上涨频率、平台惩罚合并后的分数。产品名称为"**阿尔法纯度（Alpha Purity）**" |

### 公式简化与注意事项

按当前公式，`positiveDensity + plateauRatio = 1`，因此：

\[
\text{momentumPurity} = \text{reluSlope} \times \text{positiveDensity}^2
\]

平台占比已被隐式重复惩罚。**MVP 可保留该公式**；后续回测时应验证这是否过度压低了低频但高质量的趋势股票。

---

## 六、数值示例

阈值设为 0.4%，5 日收益为：

```text
[+1.0%, -0.8%, +0.3%, +1.4%, +0.7%]
```

**ReLU 截断后**：

```text
[0.6%, 0%, 0%, 1.0%, 0.3%]
```

**累计 ReLU 动量**：

```text
[0.6%, 0.6%, 0.6%, 1.6%, 1.9%]
```

**四项指标**：

| 指标 | 计算 | 结果 |
|---|---|---:|
| reluSlope | 1.9% / 5 | **0.38%** |
| positiveDensity | 3 / 5 | **0.60** |
| plateauRatio | 2 / 5 | **0.40** |
| momentumPurity | 0.38% × 0.60 × 0.60 | **0.1368%** |

图上会看到第 1、4、5 天台阶上行，第 2、3 天平台横移。这说明正向动量存在，但中间有两天没有获得确认。

---

## 七、threshold 的实际意义

阈值是**噪声过滤器**，不是固定的"看涨标准"。

| 阈值 | 特点 |
|---|---|
| 低 | 曲线更敏感，捕获更多小涨，但容易把噪声当成动量 |
| 高 | 只保留强上涨，信号更严格，但可能漏掉缓慢上行趋势 |

**MVP 建议**：先固定在日对数收益的 **0.3% ~ 0.5%**。

**数据化阶段优化方向**：按波动率自适应：

\[
\text{threshold}_t = k \times \text{rollingVolatility}_t
\]

这样高波动股票不会因为普通日内波动频繁激活，低波动股票也不会因阈值过高而始终为零。

---

## 八、与新闻评分的结合

ReLU 模块是一个**确认因子**，而非新闻方向的替代品：

\[
\text{finalImpactScore} = 0.45 \times \text{eventScore} + 0.30 \times \text{stockRelevanceScore} + 0.25 \times \text{reluMomentumScore}
\]

### 四种组合的解读

| 新闻方向 | ReLU 动量 | 解读 |
|---|---|---|
| 利好 | 强 | 提高"高概率利好"的可信度 |
| 利好 | 弱 | 输出"利好但需确认" |
| 利空 | 强 | 不能简单视为利好，应标记"趋势与新闻冲突"风险 |
| 利空 | 弱 | 利空确认度较高 |

---

## 九、当前前端实现的偏差

现有 `ReluMomentumChart.tsx` 已用 `stepAfter` 绘制 ReLU 台阶线，但其中的 `purity` 实际计算为：

```text
positiveDays / data.length
```

即 `positiveDensity`，**并不是设计文档中的 `momentumPurity`**。

### 修正要求

正式接入后端 `ReluFactorService` 时：

- **后端**按完整公式计算并返回四项指标（`reluSlope`、`positiveDensity`、`plateauRatio`、`momentumPurity`）
- **前端**只展示结果，不自行计算纯度指标
- 避免前后端口径不一致

---

## 十、验收标准

- [ ] 输入收盘价序列、阈值、回看窗口，输出三条曲线 + 四项指标
- [ ] 对数收益率计算正确，ReLU 截断后负值与低于阈值的值均归零
- [ ] 累计动量曲线只升不降，呈阶梯形态
- [ ] 四项指标公式与设计文档一致，`momentumPurity` 非 `positiveDensity`
- [ ] 阈值默认在 0.3%~0.5% 区间，支持参数化
- [ ] 后端统一计算指标，前端仅展示，口径一致
- [ ] 模块边界清晰：只做动量确认，不做新闻方向判断与最终评分
