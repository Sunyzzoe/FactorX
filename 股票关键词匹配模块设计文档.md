# 股票关键词匹配模块设计文档

## 1. 模块概述

### 1.1 模块定位

股票关键词匹配模块的核心作用是：**将一条新闻中提到的公司、行业、产业链关键词，映射成具体股票代码，并给出匹配理由与关联度评分。**

它是新闻分析与后续金融计算之间的桥梁，在系统流程中处于承上启下的位置：

```
新闻输入
  → 事件抽取
  → 股票关键词匹配   ← 本模块
  → 拉取行情数据
  → ReLU 动量曲线
  → 影响评分
```

### 1.2 核心目标

**输入**：一条新闻或已经抽取好的事件对象

```json
{
  "companies": ["Tesla"],
  "industries": ["EV", "battery"],
  "supplyChainKeywords": ["energy storage"],
  "headline": "Tesla expands battery energy storage project in Europe",
  "body": "..."
}
```

**输出**：相关股票列表，每只股票包含代码、公司名、关联类型、关联度、命中关键词

```json
[
  {
    "symbol": "TSLA",
    "company": "Tesla",
    "relation": "直接相关",
    "relevance": 0.91,
    "matchedKeywords": ["Tesla", "battery", "energy storage"]
  }
]
```

---

## 2. 第一版技术选型：静态词典

### 2.1 为什么用静态词典

MVP 阶段不建议一开始就接入复杂的金融知识图谱或 NLP 模型。静态词典足以跑通完整闭环：

> 新闻 → 事件 → 股票 → 行情 → ReLU 曲线 → 评分

第一版词典可以写死在后端代码、JSON 文件或数据库初始化数据中，后续再逐步演进。

### 2.2 基础词典示例

| 股票代码 | 公司 | 关键词 |
|---------|------|--------|
| TSLA | Tesla | Tesla, EV, battery, solar, energy storage |
| NVDA | Nvidia | Nvidia, GPU, AI chip, data center |
| AMD | AMD | AMD, GPU, AI chip, semiconductor |
| ASML | ASML | ASML, lithography, semiconductor equipment |
| TSM | TSMC | TSMC, foundry, chip manufacturing |
| ENPH | Enphase | Enphase, inverter, solar |

### 2.3 推荐的分层结构

实际实现时，关键词应分层存储，而非全部放在一个列表中，这样才能区分不同强度的关联关系：

```json
{
  "symbol": "TSLA",
  "company": "Tesla",
  "companyKeywords": ["Tesla", "TSLA"],
  "supplyChainKeywords": ["battery", "energy storage", "solar"],
  "industryKeywords": ["EV", "renewable energy"]
}
```

三层关键词的语义：

- **companyKeywords**：公司名、股票代码、品牌名 —— 强匹配
- **supplyChainKeywords**：公司所在产业链的关键环节 —— 中匹配
- **industryKeywords**：公司所属行业 —— 弱匹配

---

## 3. 三种匹配关系

模块定义了三档关联关系，对应不同的 relevance 区间：

| 匹配情况 | 关系类型 | relevance 区间 |
|---------|---------|---------------|
| 直接出现公司名 | 直接相关 | 0.85 - 0.95 |
| 命中供应链关键词 | 产业链相关 | 0.65 - 0.80 |
| 只命中行业关键词 | 行业相关 | 0.45 - 0.65 |

### 3.1 直接相关

**定义**：新闻里直接出现公司名、股票代码或品牌名。

**示例新闻**：

> Tesla wins a battery storage contract in Europe.

**匹配结果**：

```json
{
  "symbol": "TSLA",
  "relation": "直接相关",
  "relevance": 0.92
}
```

**原因**：新闻明确提到了 Tesla，本身就是事件主体。

### 3.2 产业链相关

**定义**：新闻没直接提公司，但命中了它所在产业链的关键环节。

**示例新闻**：

> Europe increases subsidies for battery energy storage projects.

**匹配结果**：

```json
[
  {
    "symbol": "TSLA",
    "relation": "产业链相关",
    "relevance": 0.74
  },
  {
    "symbol": "ENPH",
    "relation": "产业链相关",
    "relevance": 0.68
  }
]
```

**原因**：battery、energy storage、solar 等关键词和相关公司业务有关，但新闻没有直接点名公司。

### 3.3 行业相关

**定义**：新闻只提到较宽泛的行业。

**示例新闻**：

> Global EV demand is expected to recover next year.

**匹配结果**：

```json
{
  "symbol": "TSLA",
  "relation": "行业相关",
  "relevance": 0.56
}
```

**原因**：EV 行业会影响很多公司，不一定直接影响 TSLA，因此关联较弱。

---

## 4. 匹配流程

### 4.1 总体流程

1. 合并新闻标题、正文、事件抽取结果
2. 文本标准化（小写、去标点、统一空格等）
3. 遍历股票词典
4. 分别匹配公司词、产业链词、行业词
5. 根据最高命中等级确定 relation
6. 计算 relevance
7. 去重
8. 按 relevance 降序返回

### 4.2 伪代码

```java
List<MatchedStock> match(ExtractedEvent event) {
    String text = normalize(event.headline() + " " + event.body());

    List<MatchedStock> result = new ArrayList<>();

    for (StockKeyword stock : stockDictionary) {
        MatchResult companyMatch = matchKeywords(text, stock.companyKeywords());
        MatchResult supplyChainMatch = matchKeywords(text, stock.supplyChainKeywords());
        MatchResult industryMatch = matchKeywords(text, stock.industryKeywords());

        if (companyMatch.hit()) {
            result.add(build(stock, "直接相关", 0.85, 0.95, companyMatch));
        } else if (supplyChainMatch.hit()) {
            result.add(build(stock, "产业链相关", 0.65, 0.80, supplyChainMatch));
        } else if (industryMatch.hit()) {
            result.add(build(stock, "行业相关", 0.45, 0.65, industryMatch));
        }
    }

    return result.stream()
        .sorted(Comparator.comparing(MatchedStock::relevance).reversed())
        .toList();
}
```

> **注意**：三档关系采用互斥逻辑 —— 命中公司词就不再降级判断产业链词和行业词，以最高等级为准。

---

## 5. Relevance 评分规则

### 5.1 设计原则

不使用固定分数，第一版采用**简单可解释的规则**：

> 基础分 + 命中数量加分 + 标题命中加分 + 事件主体加分

### 5.2 直接相关评分

| 加分项 | 分值 | 说明 |
|-------|------|------|
| base | 0.85 | 基础分 |
| 公司名出现在标题 | +0.05 | 标题权重更高 |
| 公司名出现在抽取出的 companies | +0.04 | 事件抽取确认 |
| 多次出现 | +0.02 | 词频信号 |
| **上限** | **0.95** | 不超过区间上限 |

### 5.3 产业链相关评分

| 加分项 | 分值 | 说明 |
|-------|------|------|
| base | 0.65 | 基础分 |
| 命中 2 个以上产业链关键词 | +0.06 | 多关键词交叉验证 |
| 出现在标题 | +0.04 | 标题权重更高 |
| 与事件行业一致 | +0.03 | 事件抽取行业匹配 |
| **上限** | **0.80** | 不超过区间上限 |

### 5.4 行业相关评分

| 加分项 | 分值 | 说明 |
|-------|------|------|
| base | 0.45 | 基础分 |
| 行业关键词出现在标题 | +0.05 | 标题权重更高 |
| 行业与事件抽取结果一致 | +0.08 | 事件抽取行业匹配 |
| **上限** | **0.65** | 不超过区间上限 |

### 5.5 可解释输出示例

```json
{
  "symbol": "NVDA",
  "company": "Nvidia",
  "relation": "产业链相关",
  "relevance": 0.76,
  "matchedKeywords": ["AI chip", "data center"],
  "reason": "新闻命中 AI chip 和 data center，属于 Nvidia 的核心产业链关键词"
}
```

---

## 6. 注意事项与设计约束

### 6.1 关键词区分强弱

- **强关键词**：如 `Nvidia`、`TSLA` —— 单独出现即可触发高置信度匹配
- **弱关键词**：如 `AI` —— 单独出现不应直接高分匹配，否则误报会很多

**建议**：弱关键词需要与其他关键词组合命中才给予较高分数，或在词典中标注权重。

### 6.2 行业词不要太泛

`technology`、`market`、`growth` 这类词不适合作为股票匹配关键词。它们太宽泛，会导致大量无意义匹配。

**原则**：行业关键词应能缩小到一个明确的产业细分领域。

### 6.3 保留 matchedKeywords

后续前端解释"为什么影响这只股票"时，需要用 `matchedKeywords` 生成说明文案。该字段是可解释性的基础，不可省略。

### 6.4 多只股票排序

一条新闻可能同时影响多只股票，应全部返回并按 relevance 降序排列。

**示例新闻**：

> New semiconductor export rules affect AI chips and lithography equipment.

**匹配结果**：

```json
[
  { "symbol": "NVDA", "relation": "产业链相关", "relevance": 0.78 },
  { "symbol": "ASML", "relation": "产业链相关", "relevance": 0.74 },
  { "symbol": "AMD",  "relation": "产业链相关", "relevance": 0.70 },
  { "symbol": "TSM",  "relation": "行业相关",   "relevance": 0.58 }
]
```

返回多只股票比只返回一只更符合真实市场逻辑。

---

## 7. 数据模型与接口定义

### 7.1 股票关键词词典模型

```java
public record StockKeyword(
    String symbol,
    String company,
    List<String> companyKeywords,
    List<String> supplyChainKeywords,
    List<String> industryKeywords
) {}
```

### 7.2 匹配结果模型

```java
public record MatchedStock(
    String symbol,
    String company,
    String relation,
    double relevance,
    List<String> matchedKeywords,
    String reason
) {}
```

### 7.3 服务接口

```java
public interface StockMatcherService {
    List<MatchedStock> match(ExtractedEvent event);
}
```

---

## 8. 验收标准

本模块完成后，应满足以下验收条件：

| 序号 | 验收项 | 说明 |
|-----|-------|------|
| 1 | 输入一条新闻，返回相关股票列表 | 至少返回 0~N 只股票 |
| 2 | 每只股票有 relation 字段 | 取值为"直接相关"/"产业链相关"/"行业相关" |
| 3 | 每只股票有 relevance 字段 | 取值在对应区间内，可解释 |
| 4 | 多只股票按 relevance 降序排列 | 最高分在前 |
| 5 | 能解释匹配原因 | 包含 matchedKeywords 和 reason 字段 |
| 6 | 可接入后续 ReLU 因子计算 | 输出格式与下游模块兼容 |

### 8.1 第一版定位

第一版重点不是"预测一定准确"，而是先做到：

- **可解释**：每只股票的匹配都有明确的关键词和理由
- **可扩展**：词典结构支持后续增加股票、调整关键词
- **能接入后续 ReLU 因子计算**：输出格式稳定，下游可直接消费

---

## 9. 后续演进方向（非 MVP 范围）

1. **关键词权重化**：为每个关键词标注权重，替代当前的分层粗粒度评分
2. **模糊匹配**：支持公司别名、简称、拼写变体的模糊匹配
3. **金融知识图谱**：接入结构化的产业链/股权/竞争关系数据
4. **NLP 实体识别**：用模型自动识别新闻中的公司实体，减少对静态词典的依赖
5. **负向关键词**：增加排除词，降低误报率（如"非 Tesla"、" unlike Nvidia"等）
6. **时效性权重**：结合新闻发布时间与行情数据，动态调整 relevance
